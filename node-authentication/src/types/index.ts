export type OAuthPayload = {
  id: string;
  name: string;
  username: string;
  email: string;
  avatar: string;
};

export type PurcahsePayload = {
  userId: string;
  diamondCategoryId: string;
};
